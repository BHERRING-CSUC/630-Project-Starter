spring.application.name=budgettracker

# Switch to file-based H2 for persistence
spring.datasource.url=jdbc:h2:file:./data/budgetdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_UPPER=false
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Postgres settings (uncomment and override when running with Postgres)
# spring.datasource.url=jdbc:postgresql://postgres:5432/budgetdb
# spring.datasource.username=postgres
# spring.datasource.password=2003sarasitha
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Fix for Codespaces redirect adding port 8080
server.forward-headers-strategy=native
